# Changelog

The canonical changelog lives at the repo root: [`CHANGELOG.md`](https://github.com/ryszard-twardy/synth-datagen/blob/main/CHANGELOG.md).

This page exists so the docs site has a navigable copy. The two are kept in sync — if they diverge, the root `CHANGELOG.md` wins.

--8<-- "CHANGELOG.md"
